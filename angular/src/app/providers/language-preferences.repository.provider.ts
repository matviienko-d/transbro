import { InjectionToken } from '@angular/core';

export const LANGUAGE_PREFERENCES_REPOS_FROM_STATE = new InjectionToken<string>(
  'language preferences repos from state token'
);

export const LANGUAGE_PREFERENCES_REPOS_TO_STATE = new InjectionToken<string>(
  'language preferences repos to state token'
);
